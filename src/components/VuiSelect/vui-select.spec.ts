import { shallowMount, createLocalVue } from '@vue/test-utils';
import StyledSelect from './vui-select';

const localVue = createLocalVue();

describe('VuiSelect.vue', () => {
  it('change locale', () => {
    // const wrapper = shallowMount(StyledSelect, {
    //   store,
    //   localVue,
    // });
    // wrapper.findAll('option').at(0).trigger('click')
  });
});
