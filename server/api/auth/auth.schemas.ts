// import { number, object, string, boolean, date, array } from "yup";

// export const SignupSchema = object({
//   email: string().trim().email().required(),
//   password: string().trim().required(),
// }).meta({
//   title: "Signup user",
//   description: "Create new user and generate tokens",
// });
